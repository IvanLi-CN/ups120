pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.{rs}"
      run: bash scripts/format_staged.sh
      stage_fixed: true
    # clippy/check disabled for workspace split; run per-crate as needed.

commit-msg:
  skip: true
