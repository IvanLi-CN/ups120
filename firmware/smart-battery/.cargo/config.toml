[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "../../scripts/probe_runner.sh"

rustflags = [
  # Linker scripts for this crate (avoid workspace-level duplication)
  "-C", "link-arg=-Tlink.x",
  "-C", "link-arg=-Tdefmt.x",
  "-C", "link-arg=--nmagic",
]

[build]
target = "thumbv6m-none-eabi"    # Cortex-M0+

[env]
DEFMT_LOG = "info"
